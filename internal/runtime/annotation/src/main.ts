// Light DOM styles are intentionally limited to annotation markers and are safe
// to link into arbitrary artifact documents as annotation.css.
import './light.css';
import { mountAnnotationApp } from './app';

mountAnnotationApp();
